const { Router } = require('express');

const carRouter = Router();

// carRouter.get('/', isDataValid, findByParam);
// carRouter.post('/', isNewUserValid, isUserPresent, addNewUser);
// carRouter.delete('/:id', isValidId, findById, deleteUser);
// carRouter.patch('/:id', isValidId, isDataValid, findById, updateUser);

module.exports = carRouter;
